{
  "name": "mod-config",
  "version": "1.0.1",
  "description": "configuration management",
  "main": "src/app.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:cerka/mod-config.git"
  },
  "scripts": {
    "test": "NODE_ENV=test jest",
    "check": "npm test && $(npm bin)/eslint src test"
  },
  "keywords": [
    "configuration",
    "yaml"
  ],
  "author": "Abel Goichon",
  "license": "ISC",
  "dependencies": {
    "node-env-configuration": "^1.0.3",
    "node-yaml": "^4.0.1"
  },
  "devDependencies": {
    "eslint": "^8.14.0",
    "jest": "^28.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "unmockedModulePathPatterns": [
      "./node_modules/"
    ]
  }
}
